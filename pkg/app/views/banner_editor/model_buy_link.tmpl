<div class="model-buy-block">
    <h2 class="banner-sub-title">{{.PostName}}</h2>
    <p class="banner-text">{{.Price}}</p>
    <a class="button button__primary banner-button" href="{{.Link}}">{{.ButtonName}}</a>
</div>