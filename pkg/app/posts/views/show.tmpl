<link type="text/css" rel="stylesheet" href="/vendors/flexslider.css" />
<link type="text/css" rel="stylesheet" href="/dist/home_posts.css" />

<div class="container posts__details">
  {{with .CurrentColorVariation }}
      <div class="grid">
        <div class="grid__col is-5 posts__gallery">

          <div class="swiper-container posts__gallery--top">
            <ul class="slides">
              {{range $image := .Images.Files}}
                <li style="background-image:url({{$image.URL}})"></li>
              {{end}}
            </ul>
          </div>

          <div class="swiper-container posts__gallery--thumbs">
            <ul class="slides">
              {{range $image := .Images.Files}}
                <li style="background-image:url({{$image.URL}})"></li>
              {{end}}
            </ul>
          </div>
        </div>

        <div class="grid__col is-7 posts__meta">
          <form id="posts__addtocart" action="/cart">
            <input name="post_id" type="hidden" value="{{.PostID}}"/>
            <input name="color_variation_id" type="hidden" value="{{.ID}}"/>

            <h1>{{.Post.Name}}</h1>
            <p class="posts__meta--price">${{.Post.Price}}</p>
            <div class="posts__meta--divider"></div>
            <ul>
              <li>
                <strong>{{ t "post.color" }}</strong>
                <div class="posts__meta--color">
                  <span class="selected" style="background: #F6841C"></span>
                  <span style="background: #25A5DF"></span>
                  <span style="background: #80DA7A"></span>
                  <span style="background: #FFD322"></span>
                  <span style="background: #77969E"></span>
                  <span style="background: #2F2F2F"></span>
                </div>
              </li>

              <li>
                <strong>{{ t "post.size" }}</strong>
                <select name="size_variation_id" style="display: none;">
                  {{range $index, $sv := .SizeVariations }}
                    <option value="{{$sv.ID}}" {{if eq $index 0}}selected{{end}}>{{$sv.Size.Name}}</option>
                  {{end}}
                </select>
                <ul class="posts__meta--size">
                    {{range $index, $sv := .SizeVariations }}
                    <li value="{{$sv.ID}}" {{if eq $index 0}}class="current selected"{{end}}>{{$sv.Size.Name}}</li>
                  {{end}}
                </ul>
                <!-- <a href="#">{{ t "post.size_guide" }}</a> -->
              </li>

              <li>
                <strong>{{ t "post.quantity" }}</strong>
                <select name="quantity">
                  <option value="1">1</option>
                  <option value="2">2</option>
                  <option value="3">3</option>
                  <option value="4">4</option>
                  <option value="5">5</option>
                </select>
              </li>
              <li>
                <button type="submit" class="button button__primary">+ {{ t "post.add_to_cart" }}</button>
                <a href="#"><img src="/images/icon-fav.png" height="24px" /> <span>{{ t "post.add_to_wishlist" }}</span></a>
              </li>
            </ul>
            <div class="posts__details--share">
              <span>{{ t "post.share" }}</span>
              <a href="#"><img src="/images/facebook-grey.png" height="24px" /></a>
              <a href="#"><img src="/images/twitter-grey.png" height="24px" /></a>
            </div>
          </form>
        </div>
      </div>

      <div class="grid posts__info">

        <div class="grid__col is-12">
          <h6>{{ t "post.description" }}</h6>
          <p>{{raw .Post.Description}}</p>
        </div>

        <div class="grid__col is-12">
          <h6>{{ t "post.specs" }}</h6>
          <div>
            <p>{{ t "post.made_in" }} {{.Post.MadeCountry}}</p>
          </div>
          <div>
            <p>{{ t "post.materials" }} Rubber soles, fabric uppers, plastic & metal zipper</p>
          </div>
        </div>

        <div class="grid__col is-12">
          <div>
            <h6>{{ t "post.framistan" }}</h6>
            <p>Me non paenitet nullum festiviorem excogitasse ad hoc. Etiam habebis sem dicantur magna mollis euismod.</p>
          </div>
        </div>
      </div>
    
  {{end}}



  <!-- featured post widget -->
  <div class="row posts__featured">
    <h3>{{ t "post.recommendations" }}</h3>
    <p>{{ t "post.recommendations_note" }}</p>
    <div class="posts__featured--slider" id="featured-post">
      <ul class="posts__list slides">
        {{range $post := related_posts .CurrentColorVariation}}
          <li>
            <a href="{{.DefaultPath}}" class="posts__list--img"><img src="{{.MainImageURL}}" /></a>
            <a class="posts__list--title" href="{{.DefaultPath}}">{{.Name}}</a>
            <p class="posts__list--price" href="{{.DefaultPath}}"><span class="item_price">${{.Price}}</span></p>
          </li>
        {{end}}
      </ul>
    </div>
  </div>
  <!-- end: featured post widget -->

  <!-- featured post widget -->
  <div class="row posts__featured">
    <h3>{{ t "post.also_bought" }}</h3>
    <p>{{ t "post.also_bought_note" }}</p>
    <div class="posts__featured--slider" id="also-bought">
      <ul class="posts__list slides">
        {{range $post := other_also_bought .CurrentColorVariation}}
          <li>
            <a href="{{.DefaultPath}}" class="posts__list--img"><img src="{{.MainImageURL}}" /></a>
            <a class="posts__list--title" href="{{.DefaultPath}}">{{.Name}}</a>
            <p class="posts__list--price" href="{{.DefaultPath}}"><span class="item_price">${{.Price}}</span></p>
          </li>
        {{end}}
      </ul>
    </div>
  </div>

  <!-- end: featured post widget -->
</div>

<script src="/vendors/jquery.flexslider-min.js"></script>
